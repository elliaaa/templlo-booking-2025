name: Issue and PR Labeler
on:
  issues:
    types: [opened, edited] # Issue가 열리거나 편집될 때 실행
  pull_request:
    types: [opened, synchronize, edited] # PR이 열리거나 동기화, 편집될 때 실행

jobs:
  label:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Label Issues and PRs Based on Title or Body
        uses: actions-ecosystem/action-add-labels@v1
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          labels: '.github/workflows/labeler.yml'
